@model MvcWeb.Models.StandardPage
@{
    ViewBag.Title = Model.Title;
}

<div class="container body-container">
    <div class="row justify-content-center">
        <div class="col-md-10 col-xs-12">
            @Html.DisplayFor(m => m.Blocks)
        </div>
    </div>
</div>
